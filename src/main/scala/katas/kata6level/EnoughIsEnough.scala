package katas.kata6level

/**
    Created by Evghenii.
    13-January-2022
*/
object EnoughIsEnough {
  //    def deleteNth(elements: List[Int], maxOccurrences: Int): List[Int] = {
  //         Code here
  //      val result = List[Int]
  var nTimes = 0
  //      elements.flatMap(x =>if ()) {
  nTimes +=1
  //        if (nTimes < maxOccurrences) result(elements) :+ elements

  //      }
  //      else result

  //    }

  class Employee(name: String,lastName: String)
  implicit val firsName = "Jon"
  val lastName = "Both"

  val employee = new Employee(firsName,lastName)

}
